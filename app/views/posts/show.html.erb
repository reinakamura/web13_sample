<h1>投稿詳細</h1>

<h2>タイトル : <%=@post.title  %></h2>

<div>本文 : <%= @post.body  %>
<% if user_signed_in? && @post.user_id == current_user.id %>
<span><%= link_to "削除", post_path(@post), method: :delete %></span>
<% end %>
</div>


<div>
	<% if user_signed_in? && @post.user_id == current_user.id %>

<%= link_to "編集する", edit_post_path(@post) %>
<% end %>

 | <%= link_to "一覧へ戻る", posts_path %></div>